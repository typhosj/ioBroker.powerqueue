/**
 * Placeholder test that keeps the mocha/ts-node/chai chain wired up.
 * Replace with the allocator and state-machine tests in phase 1.
 */

import { expect } from 'chai';

describe('scaffold', () => {
    it('runs TypeScript unit tests', () => {
        expect(true).to.equal(true);
    });
});
